# ADR Notes

Location: `docs/integration-pilot/adr/`

Naming convention:

- `NNNN-short-kebab-title` (filename pattern; append `.md` when creating the file)
- Example: `0001-oac-version-selection.md`

Sequence rule:

- Use the next available four-digit number.
- Keep numbers stable after merge.

Template:

- Copy `0000-template.md` for each new ADR.
